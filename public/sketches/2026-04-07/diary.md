今までコードの仕組みはp5.jsエディター依存だったんだけど、iframeで埋め込めばどんなhtmlファイルでもいけると知って、p5.jsエディター頼みの構成を変えた。自由が効くしね。

で、開発環境も完全にローカルに移行。最近、claude codeと一緒に何かするのにハマっているのだ。

今日も教えてもらったアルゴリズムをやってみている。noise関数を使って、ランダムに配置した座標から角度を取得。その角度方向に一定速度で進んでいくアルゴリズム。
近くにある座標のものは角度が似てくるから、パーティクルが接近するとそれらが合流して一緒に流れるようになってくるらしい。

noiseの第三引数にframeCountを足すと、地形自体が変化するので線がガタガタ揺れる。これはこれでおもしろい。trajectoryを見てみたかったので、backgroundの不透明度を落としてみた。
curl noiseは明日やってみようかな。
